apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingresas
spec:
  rules:
    - host: your-domain.com
      http:
        paths:
          - path: /
            backend:
              serviceName: nginx-service
              servicePort: 80
